<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daniel_catlett.tapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SimonTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MyTournaments" />
        <activity android:name=".AddTournament" />
        <activity android:name=".FollowedPlayers" />
        <activity android:name=".Settings" />
        <activity android:name=".TournamentPage" />
        <activity android:name=".Attendees" />
        <activity android:name=".EventAttendees" />
        <activity android:name=".FollowedSettings" />
        <activity android:name=".Schedule" />
        <activity android:name=".Events" />
        <activity android:name=".Phases"></activity>
    </application>

</manifest>